import { Command } from 'commander'

// TODO: WCH-6 — Replace with Watcher domain commands
export function registerCurrencies(
  _program: Command,
  _getJwt: () => Promise<string | undefined>,
  _output: (out: unknown) => void
) {
  // placeholder
}
