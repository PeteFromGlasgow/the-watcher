apiVersion: v1
kind: Service
metadata:
  name: kratos
spec:
  selector:
    app: kratos
  ports:
    - name: http-public
      protocol: TCP
      port: 4433
      targetPort: 4433
    - name: http-admin
      protocol: TCP
      port: 4434
      targetPort: 4434
