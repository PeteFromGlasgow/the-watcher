apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: oathkeeper-config
    files:
      - configs/oathkeeper.yaml
      - configs/access-rules.json
